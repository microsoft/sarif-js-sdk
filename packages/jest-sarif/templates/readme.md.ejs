<% matchers.forEach(matcher => { %>
### `<%- matcher.matcherName %>`

Asserts that a value is a valid SARIF <%- matcher.definition %>.

```ts
it('should be a valid SARIF <%- matcher.definition %>', () => {
  const sarif<%- matcher.type %> = buildSarif<%- matcher.type %>();

  expect(sarif<%- matcher.type %>).<%- matcher.matcherName %>();
});
```

<% }) %>